# Server environment for the BHA engine dashboard. Copy to .env for local use
# (never committed) or set each in the host's environment settings.
#
# The server reads these. Nothing here reaches the browser.

# Postgres. REQUIRED — the server prints why and exits if this is missing or
# the database is unreachable. There is no fallback store on purpose: one would
# accept writes it could not keep.
#
# On Render use the INTERNAL connection string for bha-engine-db: its host is a
# single label (dpg-...-a), it carries no TLS, and it only resolves from a
# service in the same region. The external host (....render.com) needs TLS; set
# DATABASE_CA_CERT to Render's published CA to have the certificate verified
# rather than merely used.
DATABASE_URL=postgres://user:password@localhost:5432/bha_engine
# DATABASE_CA_CERT=
# DATABASE_POOL_MAX=8

# Port the server listens on. Render sets PORT itself.
PORT=8787

# The shared team login. Produce the hash with:  npm run hash-password -- 'the password'
AUTH_EMAIL=admin@bhanetwork.org
AUTH_PASSWORD_HASH=scrypt$...$...
# Plain-text alternative for local development only. The hash wins when both are set.
# AUTH_PASSWORD=

# Signs the session cookie. Any long random string; when unset, a random key is
# generated at boot and every session ends on restart.
SESSION_SECRET=

# The Bays agent workflow and the key it expects in the x-api-key header.
ASK_BAYS_URL=https://bayshorizonnetwork.app.n8n.cloud/webhook/dashboard-ask-bays
ASK_BAYS_API_KEY=
ASK_BAYS_MODEL_LABEL=Claude Sonnet 5.0

# The n8n public API, for the Executions page (15 Sep 2026). READ ONLY — the two
# endpoints used are GET /api/v1/executions and GET /api/v1/workflows (the
# second for workflow names only), and nothing in this server can write to n8n.
#
# This is NOT the same credential as ASK_BAYS_API_KEY: that is the header a
# webhook expects, this is an instance API key (Settings → n8n API). Without it
# no execution is ever read and the Executions page says so rather than showing
# a zero.
#
# It has to be snapshotted because **n8n's execution history does not persist**:
# read on 15 Sep 2026 the instance held 3,673 executions and none older than the
# 12th. A monthly chart that asked the API about August would be told, honestly,
# that August held nothing.
N8N_API_KEY=
# Defaults to N8N_BASE_URL + /api/v1. Set this to point at a replay in a sandbox.
# N8N_API_URL=https://bayshorizonnetwork.app.n8n.cloud/api/v1

# The n8n Cloud plan's execution quota (2026-09-26). No defaults: unset, the
# Executions page says the quota is not configured and no alert is sent.
N8N_EXECUTION_QUOTA=
# A day the plan's monthly count restarted, YYYY-MM-DD or an ISO time.
N8N_BILLING_CYCLE_START=
# Slack channel id for the 70/85/95/100% alerts, posted as Bays.
QUOTA_ALERT_CHANNEL=
# Optional: Slack user ids to tag on each alert, comma-separated.
QUOTA_ALERT_MENTIONS=

# The n8n instance the "view execution" links on fixture-backed rows point at.
# Defaults to the company instance. Execution ids recorded before the move off
# n8n.arupiautomates.cloud do not exist on it and will 404; that is known.
# N8N_BASE_URL=https://bayshorizonnetwork.app.n8n.cloud

# The engine's own service key. n8n sends it in x-dashboard-key on every write
# to /api/engine/*, and since the Airtable sync was removed (13 Sep 2026) that
# is the only way a record reaches this dashboard at all.
DASHBOARD_INBOUND_KEY=

# Loop and Codex edits go straight to Airtable (14 Sep 2026), replacing the n8n
# write-back that stood here for a day. Needs data.records:read and :write on
# BOTH bases below — Open Loops and BHA Submissions.
#
# It also needs data.records:READ on five more, for the resync button that
# Build patterns, Commercial and Clients gained on 15 Sep 2026 and the two
# twins gained on 16 Sep:
#   app5ni3E8r7Lvxk22  Build Patterns
#   appvLglfdCqOKqLpT  Commercial Opportunities
#   appkSUSh9ijNjP2f8  BHA Client Research Loop
#   appRvx4u9V9BYp646  BHA North Star Ledger        (Asks)
#   appv39nQzmfC9VVkG  BHA Research Twin Ledger     (Asks AND Research Jobs)
# The last two changed on 17 Sep 2026: both twins moved onto their own ledgers,
# and the five [LEGACY] bases they used to read — appkCTjhH8PtYRFI7,
# appud969Dw7H4tMwv, app4QnMJ2woiKlLc0, appSoakKvs7MLkRnX, appxkIgnLL1zBsXqD —
# are read by nothing here now and need no grant. **A token still scoped to the
# old two authenticates and then refuses both twins' resyncs**, which is the
# same failure the submissions base had on 14 Sep, so check the scope rather
# than assuming an unchanged token still covers this.
# Nothing is ever written to those five; they have no variable of their own
# because the dashboard never writes to them and so can never write to a guess.
# Without read on them the resync refuses, names the table and changes nothing —
# it never reads a refusal as an emptied table.
#
# This said "the Open Loops base and nothing else" until 14 Sep, and a token
# scoped that way authenticates fine and then refuses every read of the
# submissions base, which is what the Codex page's reconciliation hit.
#
# Note the name: the client deleted on 13 Sep read AIRTABLE_API_KEY. This reads
# AIRTABLE_TOKEN, which is the variable set on the service.
AIRTABLE_TOKEN=
# One variable per base, named for its base. There is no default for this one:
# a default is a guess about which base real loops are written to, and a wrong
# guess writes them into it rather than failing. Unset, the server says so by
# name at boot and refuses every loop edit with the same sentence.
# (It was AIRTABLE_BASE_ID until 14 Sep 2026; that name is read by nothing.)
AIRTABLE_OPEN_LOOPS_BASE_ID=appUVlBSGGPHw6DGh
# BHA Submissions & Logs, for Codex entries. The same token covers both bases.
AIRTABLE_SUBMISSIONS_BASE_ID=appEmdKshNVTl64Zf

# Whether an edit made on a page is ALSO written to Airtable (21 Sep 2026).
#
# OFF unless this is set to 1, true, on or yes. BHA's Airtable workspace hit its
# monthly API cap on 20 Sep at about 23:00 UTC; every call from here answers 429,
# and the engine is being moved onto this dashboard's Postgres, which is now the
# record. With this off a loop or Codex edit is saved here and the page says
# nothing about Airtable — no error and no "not in Airtable" marker, because
# nothing failed.
#
# The resync buttons are deliberately NOT behind this flag. They are reads, and
# they are how the final import happens after the cap resets.
AIRTABLE_WRITEBACK=

# Whether this dashboard still depends on Airtable at all (22 Sep 2026).
#
# OFF unless set to 1, true, on or yes. Turned on only AFTER the final import
# has run and its report has been read — it is the end of the cutover, not a
# step in it. On: Engine health reports Airtable as retired rather than probing
# it, get_mirror_status reports every kind's source as the engine, the resync
# and final-import buttons come off the pages and their routes answer 410, and
# the Codex page's on-load reconciliation stops. That last one is the important
# one: its job is to DELETE rows Airtable no longer has, so left running after
# the cutover the first page load would quietly delete every Codex entry the
# engine had written since.
#
# It forces AIRTABLE_WRITEBACK off whatever that is set to. Nothing is deleted
# and the flag is reversible.
AIRTABLE_RETIRED=

# --- Engine health: the incident ledger (17 Sep 2026) -----------------------
#
# The BHARAG incident ledger, read only. This is the second external read path
# this server has, after Airtable, and it adds no dependency past Node itself.
#
# **One key per lane, and they are not interchangeable.** Each of the three
# error handlers holds its own BHARAG credential and a key for one lane is
# refused for another, so this reads three times with three keys. There are no
# defaults: a guessed key is a 401, which reads on the page like the ledger
# refusing the lane rather than like this server being unconfigured.
#
# A lane with no key here is never read, and Engine health says so rather than
# showing it healthy — no incidents and no reporting look identical from the
# outside, and only one of them is good news.
BHARAG_BAYS_KEY=
BHARAG_NORTH_STAR_KEY=
BHARAG_RESEARCH_TWIN_KEY=
# Optional. The ledger's REST base; the default is the live cluster.
# BHARAG_API_URL=https://bharag2.duckdns.org/api/v1

# The self-healing webhook, for the Retry now button on Engine health.
# A manual retry is the SAME mechanism as the 5-minute schedule's: same
# webhook, same n8n retry call, same three-attempt cap, same recording. Only
# `triggered_by` differs. Optional — the button says why it is disabled without
# it. The default is the live webhook.
# ENGINE_HEAL_URL=https://bayshorizonnetwork.app.n8n.cloud/webhook/engine-heal

# Engine health also reads two tables in the engine_events base
# (appINvgEoZjuYQI2O) with AIRTABLE_TOKEN above — error_counts and
# retry_attempts. That base needs data.records:READ on the same token.
#
# Pay Tracker reads three tables in the BHA Pay Ledger (appwnt0mEtfwDtcN5) with
# the same token — Builders, Sessions and Monthly Statements — so that base
# needs data.records:READ too. Nothing is ever written to it: paid status is set
# by the Slack card or by a statement closing, and the dashboard is read-only on
# pay by design. No base variable, for the same reason as the other read-only
# ones: this server can never write to a guess about a base it never writes to.
# Point the same client at a local replay of the API in a sandbox.
# AIRTABLE_API_URL=https://api.airtable.com/v0

# --- The MCP server (18 Sep 2026) ------------------------------------------
#
# An MCP server mounted on this same service at /mcp/<MCP_SECRET>, so an
# external Claude client can read the app's structure — routes, pages, panels,
# which data feeds which panel — and its live data. It exists because the page
# is client-rendered: fetching the dashboard's URL returns an empty shell, so a
# client asked "on this page I can't do X" could not see the app at all.
#
# READ ONLY. There is no write tool and no code path from a tool to a write.
#
# The secret is a path segment, not a header, because that is what a connector
# URL can carry. **Anything that is not exactly /mcp/<MCP_SECRET> gets a 404**,
# including every request when this variable is unset — not a 401, because a
# 401 tells a stranger the endpoint is there and that they need a credential.
#
# No default, on the same rule the Airtable base ids follow: a guessed secret
# is a secret. Unset, the boot line says so by name and /mcp/* answers 404 to
# everything.
MCP_SECRET=

# --- vFarm Early Access: the one public write route (20 Sep 2026) -----------
#
# The form on bhanetwork.org posts to POST /api/public/vfarm-early-access. That
# route is public and unauthenticated by necessity — the static site has no
# session — and it is the only one in this application. It records an
# expression of interest: no subscriber, payment, entitlement, reservation or
# delivery state is set, read or implied, and it answers `{ ok: true }` and
# nothing else, ever.
#
# The n8n webhook that posts a new lead into #vfarm-early-access. No Slack
# token lives in this repo and nothing here calls Slack directly — the webhook
# holds that credential, the same way ENGINE_HEAL_URL does.
#
# Unset, notifications are skipped silently, the boot line says so once, and
# the endpoint still works. A lead then arrives with notified_at null, which
# the Early Access tab shows on the row as "not announced".
EARLY_ACCESS_NOTIFY_URL=
# The salt for the stored ip_hash. Never the raw address, and never an
# unsalted digest: SHA-256 of an IPv4 address is reversible by anyone with an
# afternoon and four billion guesses. Unset, a random salt is generated per
# process — the rate limiter still works, but stored hashes stop comparing
# across a restart, and the boot line says so.
IP_HASH_SALT=
# Optional. Whose browser may post the form, comma-separated. The default is
# the three below, so this only needs setting to add or remove one.
# EARLY_ACCESS_ALLOWED_ORIGINS=https://bhanetwork.org,https://www.bhanetwork.org,http://localhost:5173
